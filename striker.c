#include <stdio.h>

int main(){
  float h,b,area;
  printf("Enter the height of the triangle\n");
  scanf("%f",&h);
  printf("Enter the height of the triangle\n"); 
  scanf("%f",&b);
  area=(h*b)/2;
  printf("The area of triangle is %f",area);
}